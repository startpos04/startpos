# =============================================================================
# SHARED ENVIRONMENT — web/ monorepo root
# Committed to git. Contains Docker defaults shared across all apps.
# Override locally by creating .env.local at this same level (gitignored).
# =============================================================================

# --- Infrastructure ---
POSTGRES_HOST=db
POSTGRES_PORT=5432
POSTGRES_HOST_PORT=5434
POSTGRES_USER=root
POSTGRES_PASSWORD=password
POSTGRES_DB=start-pos
