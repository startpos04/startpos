{
  "installCommand": "cd ../.. && pnpm install --frozen-lockfile",
  "buildCommand": "cd ../.. && pnpm --filter @startpos/web run build",
  "outputDirectory": ".output/public",
  "crons": [
    {
      "path": "/api/cron/daily",
      "schedule": "5 0 * * *"
    }
  ]
}
