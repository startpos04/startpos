# =============================================================================
# SHARED ENVIRONMENT TEMPLATE — web/ monorepo root
# Copy to .env and fill in values.
# These vars are shared across all apps in the monorepo.
# =============================================================================

# --- Infrastructure ---
POSTGRES_HOST=db
POSTGRES_PORT=5432
POSTGRES_HOST_PORT=5434
POSTGRES_USER=
POSTGRES_PASSWORD=
POSTGRES_DB=
