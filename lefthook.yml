# pre-commit:
#   commands:
#     biome-check:
#       glob: "*.{js,ts,jsx,tsx,json}"
#       run: pnpm exec biome check --write --no-errors-on-unmatched {staged_files}
#       stage_fixed: true
#     branch-guard:
#       run: pnpm exec tsx scripts/branch-guard.ts
#     # audit:
#     #   run: pnpm audit --audit-level moderate

# commit-msg:
#   commands:
#     lint-commit:
#       run: pnpm exec tsx scripts/lint-commit.ts "{1}"

# post-merge:
#   commands:
#     install-deps:
#       run: pnpm install --frozen-lockfile
#     generate-env:
#       run: pnpm env:generate

# pre-push:
#   commands:
#     type-check:
#       run: pnpm --filter @startpos/web exec tsc --noEmit
#     unit-tests:
#       run: pnpm --filter @startpos/web test
#     branch-guard:
#       run: pnpm exec tsx scripts/branch-guard.ts

# post-checkout:
#   commands:
#     sync-deps:
#       files: git diff --name-only HEAD@{1} HEAD
#       glob: "pnpm-lock.yaml"
#       run: pnpm install --frozen-lockfile
