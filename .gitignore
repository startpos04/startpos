node_modules
.DS_Store
dist
dist-ssr
count.txt

# ─── Env files ────────────────────────────────────────────────────────────────
# Root shared overrides
.env.local

# App-specific generated output and local overrides
apps/**/.env
apps/**/.env.local
# .env.example is committed — do not ignore it

# CI/automation config (script behavior flags — not injected into app)
.env.config

# Stripe CLI generated env (contains real secret keys)
apps/stripe/env/

# ─── Build outputs ────────────────────────────────────────────────────────────
.nitro
.tanstack
.wrangler
.output
.vinxi
.fallow/

# ─── Prisma generated (app-specific) ─────────────────────────────────────────
apps/web/prisma/generated
apps/web/prisma/schema.prisma

# ─── Test artifacts ───────────────────────────────────────────────────────────
coverage/
apps/**/coverage/
e2e-results/
apps/**/e2e-results/
playwright-report/
apps/**/playwright-report/
blob-report/
apps/**/.auth/
apps/**/playwright/.cache/

# ─── Misc ─────────────────────────────────────────────────────────────────────
todos.json
